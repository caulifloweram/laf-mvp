{
  "name": "@laf/web",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm -C ../client-web run build && pnpm -C ../broadcaster-web run build && vite build && node scripts/copy-apps.cjs",
    "dev": "vite",
    "preview": "vite preview",
    "serve": "npx --yes serve dist -p $PORT"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "typescript": "^5.6.0"
  }
}
