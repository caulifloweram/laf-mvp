[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["cd ../.. && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd packages/broadcaster-web && pnpm build"]

[start]
cmd = "cd packages/broadcaster-web && npx --yes serve dist -p $PORT"
