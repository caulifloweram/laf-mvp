{
  "name": "@laf/broadcaster-web",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "npx serve dist -p $PORT"
  },
  "dependencies": {
    "opus-decoder": "^0.7.0",
    "opus-media-recorder": "^0.8.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.2.0"
  }
}
