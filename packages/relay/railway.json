{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "if [ -f /app/packages/relay/dist/index.js ]; then cd /app/packages/relay && NODE_PATH=/app/packages/relay/node_modules:/app/node_modules node dist/index.js; else cd /app && NODE_PATH=/app/node_modules node dist/index.js; fi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100
  }
}
